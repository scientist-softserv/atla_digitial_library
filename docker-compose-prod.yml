version: '2'
services:
  web:
    image: atla_digital_library:latest
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    environment:
      - RAILS_ENV=production
      - RACK_ENV=production
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - .:/app
