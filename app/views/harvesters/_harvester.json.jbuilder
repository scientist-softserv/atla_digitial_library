json.extract! harvester, :id, :name, :base_url, :institution_name, :collection_id, :frequency, :limit, :importer, :right_statement, :thumbnail_url, :created_at, :updated_at
json.url harvester_url(harvester, format: :json)
