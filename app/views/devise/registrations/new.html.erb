<div class="page-content">
  <div class="container">
    <div class="text-center">
      <h2>Sign up</h2>
    </div>
    <div class="well well-form-wrapper center-block">
      <p>ATLA  Digital Library is not registering new user accounts at this time</p>
      <!-- <p class="lead text-center">Enter your email and password</p> -->
      <!-- <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= f.error_notification %>
        <div class="form-group">
          <%= f.label :email, :class => "col-sm-3 control-label" %>
          <div class="col-sm-9">
            <%= f.input :email, required: true, autofocus: true, :class => "form-control", label: false %>
          </div>
        </div>
        <div class="form-group">
          <%= f.label :password, :class => "col-sm-3 control-label" %>
          <div class="col-sm-9">
            <%= f.input :password, required: true, :class => "col-sm-9 form-control", label: false, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
          </div>
        </div>
        <div class="form-group">
          <%= f.label :password_confirmation, :class => "col-sm-3 control-label" %>
          <div class="col-sm-9">
            <%= f.input :password_confirmation, required: true, :class => "col-sm-9 form-control", label: false %>
          </div>
        </div>
        <div class="form-actions">
          <%= f.button :submit, "Sign up", :class => "btn btn-primary btn-lg center-block" %>
        </div>
      <% end %> -->
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
